services:
  visitka:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nadtocheev-visitka
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - "traefik.enable=true"
      # HTTP router — redirect to HTTPS
      - "traefik.http.routers.nadtocheev-http.rule=Host(`nadtocheev.ru`)"
      - "traefik.http.routers.nadtocheev-http.entrypoints=web"
      - "traefik.http.routers.nadtocheev-http.middlewares=redirect-to-https@docker"
      # HTTPS router
      - "traefik.http.routers.nadtocheev-https.rule=Host(`nadtocheev.ru`)"
      - "traefik.http.routers.nadtocheev-https.entrypoints=websecure"
      - "traefik.http.routers.nadtocheev-https.tls=true"
      - "traefik.http.routers.nadtocheev-https.tls.certresolver=letsencrypt"
      # Redirect middleware
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.permanent=true"
      # Service — loadbalancer port
      - "traefik.http.services.nadtocheev-visitka.loadbalancer.server.port=80"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
